<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>{{title}} | Tienda</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{
      --bg:#0b0c10; --card:#121318; --muted:#9aa0a6; --text:#e8eaed;
      --accent:#7c5cff; --accent-2:#00d4ff; --ok:#22c55e; --danger:#ef4444; --ring:#2a2e35;
    }
    *{box-sizing:border-box}
    body{margin:0; background:linear-gradient(180deg,#0b0c10, #0e1116); color:var(--text); font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;}
    a{color:var(--accent-2); text-decoration:none}
    a:hover{text-decoration:underline}
    nav{position:sticky; top:0; z-index:20; display:flex; align-items:center; gap:12px; padding:14px 20px; background:#0b0c10aa; backdrop-filter: blur(6px); border-bottom:1px solid var(--ring)}
    .brand{font-weight:700; letter-spacing:.4px}
    .container{max-width:1100px; margin:24px auto; padding:0 16px;}
    .row{display:flex; gap:12px; align-items:center; flex-wrap:wrap}
    .btn{background:var(--accent); color:#fff; border:0; padding:10px 14px; border-radius:10px; cursor:pointer; font-weight:600}
    .btn.secondary{background:#1f2330; color:var(--text); border:1px solid var(--ring)}
    .btn.ghost{background:transparent; border:1px solid var(--ring)}
    .badge{display:inline-block; padding:2px 8px; border-radius:999px; border:1px solid var(--ring); color:var(--muted)}
    .grid{display:grid; grid-template-columns:repeat(auto-fill,minmax(240px,1fr)); gap:16px}
    .card{background:var(--card); border:1px solid var(--ring); border-radius:16px; padding:16px; box-shadow:0 8px 24px #0004}
    .price{font-size:20px; font-weight:800}
    .muted{color:var(--muted)}
    .thumbs{display:flex; gap:8px; margin-top:8px; flex-wrap:wrap}
    .thumbs img{width:64px; height:64px; object-fit:cover; border-radius:10px; border:1px solid var(--ring)}
    .hero{display:grid; grid-template-columns:1.1fr .9fr; gap:24px}
    .hero img{width:100%; aspect-ratio: 4/3; object-fit:cover; border-radius:16px; border:1px solid var(--ring); background:#0a0b0f}
    .qty{display:flex; align-items:center; gap:8px}
    input[type=number]{background:#0f121a; color:var(--text); border:1px solid var(--ring); padding:8px 10px; border-radius:10px; width:80px}
    /* Toast */
    .toast{position:fixed; right:16px; bottom:16px; display:none; padding:12px 14px; background:#151823; border:1px solid var(--ring); color:var(--text); border-radius:12px; box-shadow:0 8px 20px #0006}
    .toast.show{display:block; animation:pop .2s ease-out}
    @keyframes pop{from{transform:translateY(10px); opacity:0} to{transform:translateY(0); opacity:1}}
    /* Pagination */
    .pagination{display:flex; align-items:center; gap:8px; margin-top:18px}
    .pill{display:inline-block; padding:6px 10px; border-radius:999px; border:1px solid var(--ring); background:#151823}
    /* Responsive */
    @media (max-width:840px){ .hero{grid-template-columns:1fr} }
  </style>
</head>
<body>
  <nav class="container">
    <div class="brand">🛒 Tienda</div>
    <a href="/products">Productos</a>
    <a id="nav-cart" href="/carts">Mi carrito</a>
  </nav>

  <main class="container">
    {{{body}}}
  </main>

  <div id="__toast" class="toast"></div>

  <script src="/public/js/shop.js"></script>
</body>
</html>

